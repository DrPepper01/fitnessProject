# Используйте официальный образ Python 3.8 как базовый образ
FROM python:3.10-slim

# Установите рабочую директорию в контейнере
WORKDIR /app

# Скопируйте файл зависимостей в рабочую директорию
COPY requirements.txt .

# Установите все зависимости из файла requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Скопируйте содержимое вашего проекта в рабочую директорию
COPY . .

# Сделайте порт 8000 доступным для мира за пределами этого контейнера
EXPOSE 8000

# Запустите приложение Django при старте контейнера
CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]